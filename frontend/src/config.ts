// Configuración de sitio que no vale la pena mover a variables de entorno de
// build (Vite/import.meta.env) porque es un único valor público, sin
// distinción entre ambientes — cambiarlo aquí y hacer un nuevo deploy.

// Número de WhatsApp Business de la empresa (+57 318 350 7127), en el
// formato que exige la URL wa.me: indicativo + número, solo dígitos.
export const WHATSAPP_NUMBER = '573183507127';
